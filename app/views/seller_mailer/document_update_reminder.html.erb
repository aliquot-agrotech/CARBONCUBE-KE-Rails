<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Document Expiry Update Reminder</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap');

    body {
      margin: 0;
      padding: 0;
      font-family: 'Inter', sans-serif;
      background-color: #f0f4f8;
      color: #333;
    }

    .wrapper {
      width: 100%;
      table-layout: fixed;
      padding: 40px 0;
    }

    .main {
      background-color: #fff;
      max-width: 600px;
      margin: 0 auto;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
    }

    .header {
      background: linear-gradient(135deg, #ffc107, #e0e0e0);
      padding: 30px;
      text-align: center;
      color: black;
    }

    .header h1 {
      margin: 0;
      font-size: 24px;
      letter-spacing: 1px;
    }

    .content {
      padding: 30px 40px;
      text-align: center;
    }

    .content h2 {
      margin-top: 0;
      color: #1a1a1a;
      font-size: 22px;
    }

    .content p {
      font-size: 16px;
      line-height: 1.6;
      color: #555;
      margin: 20px 0;
    }

    .highlight {
      font-weight: 600;
      color: #d32f2f;
    }

    .button {
      display: inline-block;
      margin-top: 25px;
      padding: 12px 28px;
      background-color: #000000;
      color: white;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 600;
      transition: background 0.3s ease, transform 0.3s ease;
    }

    .button:hover {
      background-color: #000000;
      transform: scale(1.05);
    }

    .footer {
      padding: 20px;
      text-align: center;
      font-size: 13px;
      color: #aaa;
      background: linear-gradient(135deg, #ffc107, #e0e0e0);
    }

    @media (max-width: 600px) {
      .content {
        padding: 20px;
      }

      .header h1 {
        font-size: 20px;
      }

      .content h2 {
        font-size: 20px;
      }
    }
  </style>
</head>
<body>
  <div class="wrapper">
    <div class="main">
      <div class="header">
        <h1>CarbonCube | Document Expired</h1>
      </div>
      <div class="content">
        <h2>Hello <%= @seller.fullname %>,</h2>
        <p>
          Your document
          (<span class="highlight"><%= @seller.document_type.name %></span>)
          expired on
          <span class="highlight"><%= @seller.document_expiry_date.strftime('%Y-%m-%d') %></span>.
        </p>
        <p>Please update your document to keep your account verified and active.</p>
        <a href="https://carboncube-ke.com/seller/documents" class="button">Update Document</a>
      </div>
      <div class="footer">
        &copy; <%= Date.today.year %> CarbonCube. All rights reserved.
      </div>
    </div>
  </div>
</body>
</html>
